import {strings, validationErrors} from './strings';
import colors from './colors';
import * as palette from './palette';
import images from './images';
import * as globals from './global';

const R = {
  strings,
  validationErrors,
  colors,
  palette,
  images,
  globals,
};

export {R};
