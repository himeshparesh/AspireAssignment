import {StyleSheet} from 'react-native';
import {R} from '@root/res';
const styles = StyleSheet.create({
  subContainer: {
    ...R.palette.StyleContainer(R.colors.white),
    ...R.palette.centerInPatent,
  },
});

export default styles;
